<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <div>
        <% if user_signed_in? %>
          <%= button_to "logout", '/logout', method: :delete %>
        <% else %>
          <%= button_to "keycloak login", 'http://localhost:8080/realms/app-dev', data: {turbo: false} %>
        <% end %>
    </div>
    <%= yield %>
  </body>
</html>
